{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "folder-picker",
  "description": "Capability for the main window native folder picker",
  "windows": [
    "main"
  ],
  "permissions": [
    "dialog:allow-open"
  ]
}
